// Application entry point for Console Tetris. Creates and runs the GameManager
// which handles all game logic and rendering.

using ConsoleTetris;

var gameManager = new GameManager();
gameManager.Run();
